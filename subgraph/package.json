{
  "name": "@ampleforth/subgraph",
  "version": "1.0.0",
  "dependencies": {
    "@graphprotocol/graph-cli": "0.18.0",
    "@graphprotocol/graph-ts": "0.18.1"
  },
  "license": "MIT",
  "scripts": {
    "build": "graph build",
    "codegen": "graph codegen --output-dir src/types/",
    "deploy": "graph deploy thegostep/ampleforth-subgraph --ipfs https://api.thegraph.com/ipfs/ --node https://api.thegraph.com/deploy/"
  }
}
