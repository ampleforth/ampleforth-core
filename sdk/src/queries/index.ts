import { client, MAX_PER_PAGE } from './client'
import { GET_ORACLE_DATA, GET_PROVIDER_REPORTS } from './oracle'
import { GET_POLICY_DATA, GET_REBASE_REPORTS } from './policy'
import { GET_TOKEN_DATA, GET_TOKEN_BALANCES, GET_TOKEN_APPROVAL } from './token'

export {
    client,
    MAX_PER_PAGE,
    GET_ORACLE_DATA,
    GET_PROVIDER_REPORTS,
    GET_POLICY_DATA,
    GET_REBASE_REPORTS,
    GET_TOKEN_DATA,
    GET_TOKEN_BALANCES,
    GET_TOKEN_APPROVAL,
}
